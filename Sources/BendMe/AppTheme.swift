import SwiftUI

/// Shared light palette for the native settings and setup windows.
enum AppTheme {
    static let accent = Color.blue
    static let background = Color(white: 0.985)
    static let sidebar = Color(white: 0.95)
    static let card = Color.white
    static let subtle = Color.black.opacity(0.035)
    static let selection = Color.black.opacity(0.065)
    static let border = Color.black.opacity(0.08)
}
