# Current state

The Apple-notarized direct release is published at https://winsonbaring.github.io/bendme/ and GitHub v0.1.0. Issue #4 completed: app signature, Apple ticket, Gatekeeper, mounted DMG and public download checks passed. The automated finisher completed successfully; local main includes its release commits. App Store submission remains separate under #2.

User requested a complete local uninstall for a manual fresh-install test (issue #5). Both running BendMe instances are stopped; three mounted installers were ejected. /Applications/BendMe.app and dist/BendMe.app are removed. All public/development/diagnostic privacy grants were reset, defaults cleared, and build copies unregistered from Launch Services. Source, DMG/ZIP release files and notarization artifacts are preserved.

Two old diagnostic sandbox containers retain only protected macOS metadata files; their Data directories were removed. Command-line metadata removal was denied by macOS; Finder removal timed out. Complete removal of those two empty test containers remains unresolved. These use separate sandboxcheck identifiers, not the public app identifier, and contain no remaining BendMe preferences or user data. The public installation is ready for a new Screen Recording prompt on the next installation. See docs/VERIFICATION.md for earlier build and capture evidence; previous running/authorized-app notes no longer describe current local state.
